# S00 — Project shell

**Status:** green  
**After:** nothing  
**Cite:** REQUIREMENTS §6.6, §16.1, §16.4, §18; ADR-001, ADR-002, ADR-003  
**Forbidden:** masonry generator, analysis, physics, extra libraries, 3D viewport

## Done when

- [x] `npx vitest run tests/slices/s00.test.ts` passes
- [x] Load [docs/fixtures/s00-empty/project.json](../fixtures/s00-empty/project.json); save round-trips (same numbers; key order may differ)
- [x] Saved JSON validates against `project.schema.json` and includes `requirementsVersion: "1.4"`
- [x] Display units default **US customary**; stored numbers stay SI (ADR-003)
- [x] Ground line at `y = 0` is visible; scale bar visible

## Given

Empty project: no vertices. Schema defaults: `units: us-customary`, `sliceDepth: 1`, `bondPattern: common`, `jointMode: lime` (US modular 3/8 in mortar). Core binder default remains **dry** when a core is later drawn (DEC-045) — not in this slice.

## When

App boots; user saves; user reloads.

## Then

Match [docs/fixtures/s00-empty/expected.json](../fixtures/s00-empty/expected.json).

## Test command

`npx vitest run tests/slices/s00.test.ts`

## Stop

Update STATUS.md. Do not start S01 if context is near 100k tokens.
