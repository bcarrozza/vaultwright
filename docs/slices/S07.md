# S07 — Optimize preview and Apply

**Status:** green  
**After:** S06 green  
**Cite:** REQUIREMENTS §13, DEC-008–012, DEC-035, DEC-052  
**Forbidden:** Shrinking void or walls/deck/fill; piercing the deck; mutating outline before Apply; rise above semicircle default; flying buttresses

## Done when

- [x] `npx vitest run tests/slices/s07.test.ts` passes
- [x] Live preview of axis, `t`, H; **Apply** commits outline (DEC-052)
- [x] Minimize `t` then snap up to `N * d_orient`; odd `Nv`
- [x] Rise cap default `semicircle`; void area unchanged within 1e-6 m²; deck not pierced
- [x] Initial (dashed) vs optimized (solid) overlay; inspector deviation
- [x] `VOID_LOCK` if a trial would violate DEC-035 — preview refuses, does not apply

## Given

S03/S04 ring with a labeled void. Rise-cap mode `semicircle`.

## When

User runs Optimize, inspects preview, Apply or Cancel.

## Then

Void lock holds; `t` does not increase without a documented snap-up; Cancel leaves JSON identical.

## Test command

`npx vitest run tests/slices/s07.test.ts`

## Stop

STATUS.md. Buttress sizing is S08.
