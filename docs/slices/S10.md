# S10 — Auto footings and frost display

**Status:** green  
**After:** S09 green  
**Cite:** REQUIREMENTS §12.3, DEC-037, DEC-038, DEC-048, DEC-049  
**Forbidden:** Rebar, piles, grade beams, frost-heave simulation, overwriting `userEdited` footings

## Done when

- [x] `npx vitest run tests/slices/s10.test.ts` passes
- [x] Stepped spread footing under every wall/pier/buttress that lacks one
- [x] Auto update when H/loads change unless `userEdited`
- [x] Checks: bearing, middle third of base, slide with soil μ only, frost line vs depth
- [x] Default soil stiff clay/firm sand 150 kPa, μ 0.40; default depth **4 ft**; default frost preset **36 in**
- [x] `FROST_SHALLOW` when depth < frost line; `FOOTING_USER_EDITED` when locked
- [x] Regenerate rebuilds non-edited, or all after confirm

## Given

S08/S09 model with ground line = soil surface.

## When

Generate; change H; edit one footing; change H again; toggle frost preset.

## Then

Edited footing unchanged (may go red). Display OK / too shallow. No heave physics.

## Test command

`npx vitest run tests/slices/s10.test.ts`

## Stop

STATUS.md.
