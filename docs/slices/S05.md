# S05 — Joints and crushing

**Status:** green  
**After:** S04 green  
**Cite:** REQUIREMENTS §7.4–7.5, DEC-004, DEC-021, DEC-042, DEC-047  
**Forbidden:** Tension in mortar; coupling joint mode to core binder; rebar

## Done when

- [x] `npx vitest run tests/slices/s05.test.ts` passes
- [x] Dry: `|V| ≤ μN`, no cohesion, pack joint 2 mm
- [x] Lime: `|V| ≤ cA + μN`, default `c = 0.05 MPa`, joint 10 mm
- [x] Crushing mark when peak compressive stress > allowable table (§7.5)
- [x] Switching only joint mode re-runs checks without redrawing the outline
- [x] Inspector: max stress, allowable, ratio, location

## Given

S04 model plus a tabulated dry case that **slides** at μ = 0.45 and **holds** at μ = 0.75 (record numbers in the S05 fixture when this slice starts).

## When

Toggle Dry | Lime; override μ, c, allowable.

## Then

Codes `SLIDING` and `CRUSHING` match the table. Overlay pass still requires all green (S04 rule).

## Test command

`npx vitest run tests/slices/s05.test.ts`

## Stop

STATUS.md. No live-load envelope yet.
