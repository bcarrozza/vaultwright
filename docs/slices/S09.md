# S09 — Multi-span and stacked arches

**Status:** green  
**After:** S08 green  
**Cite:** REQUIREMENTS §11, DEC-006  
**Forbidden:** Merging two member lines into one polyline; groin vaults; auto-boolean overlapping rings

## Done when

- [x] `npx vitest run tests/slices/s09.test.ts` passes
- [x] Each arch has a **stable color** and a full-length line
- [x] Shared pier: vector addition; symmetric loads → H cancels; cart on one bay → `LOAD_UNBALANCE` and residual H in pier kern
- [x] Stacked: upper supports are loads on the lower extrados; two lines, not one curve
- [x] Overlapping `archRing` faces: `RING_OVERLAP`, block generation

## Given

Preset-like two-span arcade; then a stacked pair.

## When

Envelope includes asymmetric live. Analyze.

## Then

Separate lines; residual H visible; overlap blocked.

## Test command

`npx vitest run tests/slices/s09.test.ts`

## Stop

STATUS.md. Relieving-arch SHOULD is allowed here if cheap; do not block S10 on it.
